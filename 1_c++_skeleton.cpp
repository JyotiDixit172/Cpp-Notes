#include <iostream>
using namespace std;
// shortcut which c++ organizes it's standard tools (like the used for printing)
// inside a container called std
// this line sets those tools directly without having to prefix them with std:: every time.

// this is like starting point where, when we run a program the machien looks for main()
// and starts executing from there.

// the {} "curly braces" marks the begninning & end of the main function. 
// Everything inside it gets executed sequentially
int main(){

    // write your code here 
    return 0; // for garbage collection ; also it tells the machine that program is finished succesfully
}
